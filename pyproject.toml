[project]
name = "bitguard-sentinel"
version = "0.2.0"
description = "Agent risk and order audit dashboard for Bitget trading agents."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
bitguard = "bitguard.cli:main"

[tool.setuptools]
packages = ["bitguard"]
